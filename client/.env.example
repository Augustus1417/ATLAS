# Copy to .env or .env.local in this folder (same level as vite.config.js)
# IMPORTANT: If .env.local exists, it OVERRIDES .env (common cause of still seeing localhost).
# Restart `npm run dev` after any change — Vite only reads env at startup.
VITE_API_BASE_URL=http://localhost:8000
